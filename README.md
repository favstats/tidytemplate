# Tidytemplate

```r
# install.packages("devtools")
devtools::install_github("favstats/tidytemplate")
```

This is a package that mostly use for personal reasons

The package contains an .Rmd template and a few helper function.